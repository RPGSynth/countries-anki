# Natural Earth Inputs

Place pinned Natural Earth shapefiles here:

- `ne_10m_admin_0_countries.shp` (+ sidecars)
- `ne_10m_populated_places.shp` (+ sidecars)

The config expects these exact filenames by default.
